#include <stdio.h>
/**
 * main - main
 * @argc: the number of arguments
 * @argv: the arguments array
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
